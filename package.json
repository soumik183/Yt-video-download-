{
  "name": "yt-downloader-vercel-friendly",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir public && cp -r components/ utils/ public/ && cp index.html index.tsx App.tsx types.ts metadata.json public/"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}